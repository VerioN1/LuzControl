import React from 'react';
import Button from '@mui/material/Button';

const Home = () => {
  const hello = 'helo';
  return (
    <div style={{}} className="fullscreen-flex">
      <Button>asdk</Button>
    </div>
  );
};

export default Home;
